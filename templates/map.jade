extends base

block content
  #map(ng-controller='MapCtrl')
    .google-map(google-map, style='width:100%;height:400px;')
      span.loading Google Map to be loaded here
    section.current-pub
      h1 {{current.name}}
      url {{current.url}}
      ul.beers(ng-if='beers')
        li(ng-repeat='beer in beers') {{beer.name}}
      p.no-beers(ng-if='!beers') No beers bro
        form
          label(for='beer') Beer
          input#beer(type='text')
          input(type='text', value='')
          button(type='button', ng-click='searchBoozes()') Search
          ul.search-beers(ng-if='booze_recommendations')
            li(ng-repeat='beer in booze_recommendations')
          button(type='submit') Add beer
